#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[])
{
    int t;
    int n, k, p;

    scanf ("%d", &t);

    while (t--) {
        scanf ("%d %d %d", &n, &k, &p);

        
    }

    return 0;
}
